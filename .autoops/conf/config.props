#URL where this app is saved
baseLink=http://localhost:8090/
#Output files link for the email
output_folder=http://localhost:8090/serve/

#Options for below home or absolute
baseLocation=absolute

#If the above is home, this will get appended to system.home, else this will be used as absolute path
share_location=serve/

#Accept and reject URLs
accept_url=#/approve
reject_url=reject

#Emails
from_DL=autoops@yourcompany.com
bcc_DL=autoops@yourcompany.com

#Don't delete the below, this can be used when org wide email is used
emailPostFix=

#JDBC connection
jdbc_connection_string=jdbc:h2:tcp://localhost/~/.autoops/db/oat;IFEXISTS=TRUE;USER=oat;PASSWORD=123456

#Key
key=nn0Z1yL9dRJ5zS+yH+tsb8rxuJM665Uu14Qi5t3MTes=